spring:
  datasource:
    url: jdbc:mysql://localhost:13306/stock_lab?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: stocklab
    password: stocklab

toss:
  base-url: https://openapi.example.invalid
  client-id: test-client-id
  client-secret: test-client-secret
  auth:
    # 중요: 테스트가 컨텍스트를 띄울 때마다 토큰을 발급하면
    # 토스 토큰은 클라이언트당 1개뿐이라 실행 중인 배치의 토큰이 즉시 죽는다.
    eager-init: false

logging:
  level:
    dev.teolab.stocklab: DEBUG
